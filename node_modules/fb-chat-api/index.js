const login = require('./src/login');
const api = require('./src/api');

(async () => {
  try {
    const appState = await login();
    await api(appState);
  } catch (err) {
    console.error("Login failed:", err);
  }
})();
