const fs = require('fs');
const path = require('path');

module.exports = function login() {
  return new Promise((resolve, reject) => {
    try {
      const appState = require(path.join(__dirname, '../appstate.json'));
      resolve(appState);
    } catch (err) {
      reject("appstate.json not found or invalid.");
    }
  });
};
