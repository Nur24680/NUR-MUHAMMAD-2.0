module.exports = {
  config: {
    name: "ready"
  },

  run: async ({ api }) => {
    console.log("🤖 SHOUROV-BOT is ready!");
    // চাইলে এখানে autostart message, group post, etc করতে পারো
  }
};
